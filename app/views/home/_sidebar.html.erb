<div class="sidebar">
	<ul>
		<% Category.all.each do |category| %> 
			<li>
			<% if category.id.to_s != params[:id] %>
				<%= link_to category.name, action: 'show', id: category.id %>
			<% else %>
				<%= link_to category.name, { action: 'show'}, id: category.id, class: "selected" %>
			<% end %>
			</li>
		<% end %>
	</ul>
</div>
