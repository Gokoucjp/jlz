<div>
	<% @pictures.each do |p| %>
		<%= image_tag p.path, alt: p.name, class: "list_picture"%>
		<%= link_to 'edit', edit_picture_path(p) %>
		<%= link_to 'delete', p, method: :delete, data: { confirm: '确定要删除吗?' } %>
	<% end %>
</div>
